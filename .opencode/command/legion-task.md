---
description: "Manage LEGION tasks: list, create, update, or complete"
---
$ARGUMENTS

Help me manage LEGION tasks. Based on my input:
- "list" → use `listTasks` to show tasks for the current engagement
- "create [title]" → use `createTask` with the given title, ask for description
- "complete [id]" → use `completeTask` for the given task
- "update [id] [status]" → use `updateTask` to change status
- Otherwise, show task help
